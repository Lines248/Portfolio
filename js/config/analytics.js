export const analyticsConfig = {
  clarity: {
    projectId: "udtn6czofq", //Clarity Project ID
    enabled: true,  // Set to false to disable Clarity
    loadAfterInteraction: true,  // Load after user interaction (scroll/click)
    loadDelay: 3000,  // Minimum delay before loading (ms) - gives priority to page rendering
  }
};

